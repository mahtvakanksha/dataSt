#include <iostream>
#include <math.h>
#include <string.h>
using namespace std;

int main()
{
    cout << "enter ";
    /*int a, b;

    cin >> a >> b;
    cout << (a & b);
    cout << (~a);
    cout << (a ^ b);
    cout << (a | b);
*/
    /*
    fibonacci series
    int a = 0, b = 1, n, sum = 0;
    cin >> n;
    cout << a << "\t" << b;
    for (int i = 1; i <= n; i++)
    {
        sum = a + b;
        cout << "\t" << sum;
        a = b;
        b = sum;
    }
    */

    // decimal to binary
    int n;
    cin >> n;
    /*

     int ans = 0;
    int i = 0;
    while (n != 0)
    {
        int bit = n & 1;
        ans += (bit * pow(10, i));
        n = n >> 1;
        i++;
    }
    cout << ans;*/

    // negative decimal to binary:

    return 0;
}
